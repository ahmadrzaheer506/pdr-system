{
  "name": "pdr-system",
  "version": "1.0.0",
  "description": "Paul Douglas Roofing & Building Ltd — Unified Business Operating System (CRM, Scheduling, AI, Quoting, Invoicing)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "setup": "node setup.js",
    "check": "node check.js",
    "test": "node test-uktax.js && node e2e-test.js && node test-features.js",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "seed": "node server/seed.js",
    "seed:clean": "node server/seed.js --clean",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
